# Trained Models

This folder contains the serialized trained XGBoost model used in the project.

If downloading from the ZIP archive, extract `enhanced_model.pkl` before use.

The model can also be regenerated by running `src/model_training.py`.
